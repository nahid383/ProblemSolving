#include<stdio.h>
#include<math.h>
int main()
{
    double value, x;
        printf("Enter the value of X: ");
        scanf("%lf",&x);
        value=sin(x);
        printf("sinx= %lf",value);
        return 0;
}
