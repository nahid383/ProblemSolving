#include<stdio.h>
#include<string.h>
#include<math.h>
int main()
{
    int t;
    scanf("%d",&t);
    while(t--)
    {
        char atoz[26];
        char word[51];
        int minimul_times=0;
        scanf("%s",atoz);
        scanf("%s",word);
        int length = strlen(word);
        for(int i=0; i<26; i++)
        {
            for(int j=0; j<length; j++)
            {
                if(atoz[i]==)
            }
        }
    }
}