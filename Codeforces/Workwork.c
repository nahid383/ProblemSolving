#include<stdio.h>
int main()
{
    int t;
    scanf("%d",&t);
    while(t--)
    {
        int n;
        scanf("%d",&n);
        char array[n];
        for(int i=0; i<n; i++)
        {
            scanf("%d",&array[i]);

        }
    }
}
